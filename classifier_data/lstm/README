This folder contains LSTM sepcific data. 

networks/
	Contains trained networks. Properties are encoded in filename, error rate is last parameter. 
	In repository three networks are provided. 
	- First a network from the general test for parameters (Table A.5 in document)
	- Second a network trained only with Data from gestures from Frank
	- Third a network trained only with Data from gestures from Daniel
	

data/
	Contains datasets. Datasets are not saved in Repository. 
	
gestures/
	Contains used gestures. Ensure that your gesture path points to this folder instead of gestures/ in root folder. 
	
